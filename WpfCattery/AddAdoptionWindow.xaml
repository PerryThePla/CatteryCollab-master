<Window x:Class="WpfCattery.AddAdoptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aggiungi Adozione" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Seleziona Gatto:" Margin="0,0,0,5"/>
            <ComboBox x:Name="CBCats" DisplayMemberPath="name" Height="25"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Seleziona Adottante:" Margin="0,0,0,5"/>
            <ComboBox x:Name="CBAdopters" Height="25">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding FirstName}"/>
                            <Run Text=" "/>
                            <Run Text="{Binding LastName}"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding FiscalCode}"/>
                            <Run Text=")"/>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Data Adozione:" Margin="0,0,0,5"/>
            <DatePicker x:Name="DPAdoptionDate" SelectedDateFormat="Short"/>
        </StackPanel>

        <Button x:Name="BTNAggiungi" Grid.Row="4" Content="Aggiungi Adozione" Width="150" Click="BTNAggiungi_Click"/>
    </Grid>
</Window>